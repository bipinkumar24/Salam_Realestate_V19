<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_buruuj_rate_claude_generate_form" model="ir.ui.view">
        <field name="name">buruuj.rate.claude.generate.form</field>
        <field name="model">buruuj.rate.claude.generate</field>
        <field name="arch" type="xml">
            <form string="Generate Master Rates with Claude">
                <sheet>
                    <div class="oe_title">
                        <h1>Generate Master Rates with Claude</h1>
                        <p class="text-muted">
                            Drafts are inserted with Active = unchecked. Review them in
                            the Master Rate Database, tick "Active" on the keepers, and
                            archive or delete the rest.
                        </p>
                    </div>
                    <group>
                        <group>
                            <field name="trade_id" options="{'no_create': True}"/>
                            <field name="category"/>
                            <field name="region"/>
                            <field name="item_count"/>
                        </group>
                    </group>
                    <group string="Extra context (optional)">
                        <field name="extra_context" nolabel="1"
                               placeholder="e.g. Q3 2026 prices, high-rise concrete works, scaffolded environment..."/>
                    </group>
                </sheet>
                <footer>
                    <button name="action_generate" string="Generate" type="object" class="btn-primary"/>
                    <button string="Cancel" class="btn-secondary" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

    <record id="action_buruuj_rate_claude_generate" model="ir.actions.act_window">
        <field name="name">Generate with Claude</field>
        <field name="res_model">buruuj.rate.claude.generate</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
    </record>

</odoo>
